export enum Gender {
  Male = 'male',
  Female = 'female',
}

export enum SubjectType {
  Chapter = 'chapter',
  Unit = 'unit',
  Lesson = 'lesson',
}

export enum QuestionType {
  TrueFalse = 'true-false',
  MultiChoice = 'multi-choice',
  MultiAnswers = 'multi-answers',
}
